# Leave DATABASE_URL unset for local SQLite. Production requires PostgreSQL.
# DATABASE_URL=postgresql://user:password@localhost:5432/onzenon
PORT=3001
APP_ORIGIN=http://localhost:3001
TRUST_PROXY_HOPS=0
# First administrator only. Set through a secure environment; remove the password after creation.
# ADMIN_EMAIL=you@example.com
# ADMIN_PASSWORD=choose-a-unique-password-with-at-least-14-characters
